#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	setlocale(LC_ALL, "rus");
	float z, n, b;
	bool  rule = false;
	cout << "Ââĺäčňĺ n: ";
	cin >> n;
	cout << "Ââĺäčňĺ z: ";
	cin >> z;
	for (int i = 0; i < n; i++)
	{
		if (n > 0)
		{
			b = sin(n + i / n);
			cout << b << " ";
			if (b == z)
			{
				rule = true;
				break;
			}
		}
	}
	if (rule)
	{
		cout << "Äŕ";
	}
	else
	{
		cout << "Íĺň";
	}

}
